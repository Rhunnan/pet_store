import 'package:flutter/material.dart';

class ItemsPet extends StatelessWidget {
  const ItemsPet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
